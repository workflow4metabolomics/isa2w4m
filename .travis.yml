notifications:
 email:
  recipients:
   - pk.roger@icloud.com

language: python

python:
    - "3.6"

install:
    - sudo apt-get update
    - sudo apt-get install -y python3-pip
    - git clone -b feat/w4m-tests https://github.com/pkrog/isa-api.git
    - pip3 install -e isa-api

branches:
  only:
    - master
    - develop

script:
    - make test
    - make planemolint
